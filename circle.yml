machine:
  ruby:
    version: 2.1.2
  node:
    version: v0.10.28
dependencies:
  post:
    - npm install bower -g
    - bower install
general:
  artifacts:
    - coverage-out/*.html
    - log/*.log
    - npm-debug.log
